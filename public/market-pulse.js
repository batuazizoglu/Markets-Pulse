const mpStyle=document.createElement('style');
mpStyle.textContent=`
#market-pulse-section{margin-top:0;scroll-margin-top:90px}.mp-shell{display:grid;gap:12px}.mp-hero{display:grid;grid-template-columns:280px minmax(0,1fr);gap:12px}.mp-pressure,.mp-summary,.mp-mini,.mp-threats,.mp-mix{background:var(--surface);border:1px solid var(--line);border-radius:14px;box-shadow:var(--shadow)}.mp-pressure{padding:18px;display:flex;align-items:center;gap:18px;min-width:0}.mp-pressure-copy{min-width:0}.mp-ring{--score:0;--pressure-color:var(--bad);width:108px;height:108px;border-radius:50%;display:grid;place-items:center;background:conic-gradient(var(--pressure-color) calc(var(--score)*1%),var(--line) 0);position:relative;flex:0 0 auto}.mp-ring.low{--pressure-color:var(--good)}.mp-ring.med{--pressure-color:var(--warn)}.mp-ring.high{--pressure-color:var(--bad)}.mp-ring:after{content:"";position:absolute;inset:10px;background:var(--surface);border:1px solid var(--line);border-radius:50%;box-shadow:inset 0 0 0 1px color-mix(in srgb,var(--ink) 4%,transparent)}.mp-ring strong{position:relative;z-index:1;font-size:28px;letter-spacing:-.04em;color:var(--ink);font-weight:900;text-shadow:none}.mp-pressure-copy span,.mp-label{display:block;color:var(--muted);font-size:10px;text-transform:uppercase;letter-spacing:.07em;font-weight:800}.mp-pressure-copy b{display:block;font-size:17px;margin-top:4px}.mp-pressure-copy small{display:block;color:var(--muted);margin-top:3px}.mp-summary{padding:20px;min-width:0;overflow:hidden}.mp-summary h2{margin:4px 0 7px;font-size:22px;letter-spacing:-.025em;line-height:1.15;color:var(--ink);overflow-wrap:anywhere}.mp-summary p{margin:0;color:var(--muted);max-width:900px;line-height:1.55;overflow-wrap:anywhere}.mp-badges{display:flex;gap:6px;flex-wrap:wrap;margin-top:13px;min-width:0}.mp-badge{padding:5px 8px;border-radius:999px;background:var(--surface2);color:var(--ink);border:1px solid var(--line);font-size:10px;font-weight:800;max-width:100%;overflow-wrap:anywhere}.mp-kpis{display:grid;grid-template-columns:repeat(4,1fr);gap:10px}.mp-mini{padding:14px}.mp-mini strong{display:block;font-size:22px;margin-top:5px;letter-spacing:-.03em}.mp-mini small{color:var(--muted)}.mp-main{display:grid;grid-template-columns:minmax(0,1.65fr) minmax(300px,.75fr);gap:12px}.mp-head{display:flex;align-items:center;justify-content:space-between;gap:10px;padding:13px 15px;border-bottom:1px solid var(--line)}.mp-head strong{font-size:14px}.mp-list{padding:4px 14px 12px}.mp-move{padding:12px 2px;border-bottom:1px solid var(--line)}.mp-move:last-child{border-bottom:0}.mp-move-top{display:flex;align-items:flex-start;justify-content:space-between;gap:12px}.mp-move-title{font-weight:850}.mp-meta{font-size:10px;color:var(--muted);margin-top:2px}.mp-score{min-width:54px;text-align:center;border-radius:10px;padding:7px 8px;font-weight:900;background:var(--badbg);color:var(--bad)}.mp-score.med{background:var(--warnbg);color:var(--warn)}.mp-score.low{background:var(--goodbg);color:var(--good)}.mp-reasons{display:flex;flex-wrap:wrap;gap:5px;margin-top:8px}.mp-reason{font-size:9px;padding:4px 6px;border-radius:7px;background:var(--surface2);color:var(--ink);border:1px solid var(--line)}.mp-action{margin-top:8px;padding:9px 10px;border-radius:9px;background:var(--surface2);font-size:11px;color:var(--ink);border:1px solid var(--line)}.mp-action b{color:var(--ink)}.mp-mixes{padding:12px 14px}.mp-mix-group+.mp-mix-group{margin-top:16px}.mp-mix-title{font-size:10px;color:var(--muted);text-transform:uppercase;letter-spacing:.06em;font-weight:800;margin-bottom:8px}.mp-bar-row{display:grid;grid-template-columns:minmax(90px,1fr) 2fr 38px;gap:8px;align-items:center;margin:7px 0;font-size:11px}.mp-bar{height:7px;border-radius:999px;background:var(--line);overflow:hidden}.mp-bar i{display:block;height:100%;background:var(--blue);border-radius:999px}.mp-bar-row em{font-style:normal;text-align:right;color:var(--muted)}.mp-note{font-size:10px;color:var(--muted);padding:0 2px}.mp-empty{padding:28px;color:var(--muted);text-align:center}.mp-window{display:flex;gap:5px}.mp-window button{border:1px solid var(--line);background:var(--surface);border-radius:8px;padding:5px 8px;font-size:10px;font-weight:800;color:var(--muted);cursor:pointer}.mp-window button.active{background:var(--navy);color:#fff;border-color:var(--navy)}
@media(max-width:1120px){.mp-hero,.mp-main{grid-template-columns:1fr}.mp-kpis{grid-template-columns:repeat(2,1fr)}}
@media(max-width:720px){.mp-pressure{padding:14px}.mp-ring{width:88px;height:88px}.mp-ring strong{font-size:23px}.mp-summary{padding:15px}.mp-summary h2{font-size:18px}.mp-kpis{gap:8px}.mp-mini{padding:12px}.mp-mini strong{font-size:18px}.mp-move-top{align-items:center}.mp-head{align-items:flex-start;flex-direction:column}.mp-window{width:100%;overflow:auto}}
.market-status,.market-help{font-size:12px;line-height:1.55;color:var(--muted);overflow-wrap:anywhere;margin:8px 0}.market-status.error-text{color:var(--bad);max-width:none}.market-list-footer{display:flex;align-items:center;justify-content:space-between;gap:12px;flex-wrap:wrap;padding:12px 14px;border-top:1px solid var(--line);font-size:12px;color:var(--muted)}.market-list-footer [hidden]{display:none}.mp-tools{display:flex;justify-content:space-between;align-items:center;gap:12px;flex-wrap:wrap;padding:12px 14px}.mp-tools label{font-size:12px;color:var(--muted)}.mp-tools select{margin-left:7px;background:var(--surface);border:1px solid var(--line);border-radius:8px;color:var(--ink);padding:7px;font:inherit;max-width:100%}.mp-tools a{color:var(--blue);font-size:12px}.mp-change-list{padding-left:20px;font-size:12px;line-height:1.55;overflow-wrap:anywhere}.mp-change-list li+li{margin-top:5px}.mp-threats>.market-help{padding:0 14px}.mp-meta,.tl-content,.change-title,.diff{overflow-wrap:anywhere}.mp-meta{font-size:11px}.mp-score small{display:block;font-size:9px;font-weight:600}.mp-move-actions{font-size:12px}.mp-list .mp-move-title{font-size:14px}#changes-section .panel-head{flex-wrap:wrap}#timelineTabs{width:auto;flex-wrap:wrap}.market-list-footer .btn{height:auto;min-height:38px;padding:8px 12px;white-space:normal}#mpSnapshotStatus{margin:0}
@media(max-width:720px){.actions .btn[onclick="loadAll()"]{display:inline-flex;align-items:center}.topbar-inner{flex-wrap:wrap}.mp-tools label{width:100%}.mp-tools select{max-width:calc(100% - 70px)}}
`;
document.head.appendChild(mpStyle);

function mpEsc(s){return String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}
function mpTime(v){return v?new Intl.DateTimeFormat('tr-TR',{dateStyle:'short',timeStyle:'short',timeZone:'Asia/Famagusta'}).format(new Date(v)):'—'}
let mpLimit=6,mpOrder='priority',mpWindow=null;

async function installMarketPulse(){
  await window.MarketPulseAccess?.ready;
  document.title='Markets Pulse by Turkcell';
  const nav=document.querySelector('.section-nav');
  if(nav&&!nav.querySelector('a[href="#market-pulse-section"]')) nav.insertAdjacentHTML('afterbegin','<a href="#market-pulse-section">Markets Pulse</a>');
  const overview=document.getElementById('overview');
  if(overview&&!document.getElementById('market-pulse-section')) overview.insertAdjacentHTML('beforebegin',`<section id="market-pulse-section" class="mp-shell"><div id="mpContent" class="mp-empty">Markets Pulse hesaplanıyor…</div></section>`);
  document.getElementById('market-pulse-section')?.addEventListener('click',event=>{
    if(event.target.closest('[data-market-more="moves"]')){mpLimit+=6;renderMarketState(window.MarketPulseData.getState())}
  });
  document.getElementById('market-pulse-section')?.addEventListener('change',event=>{if(event.target.id==='mpMoveOrder'){mpOrder=event.target.value==='recent'?'recent':'priority';renderMarketState(window.MarketPulseData.getState())}});
  window.MarketPulseData.subscribe(renderMarketState);window.MarketPulseData.ensure();
}

function loadMarketPulse(days=window.MarketPulseData.getState().days){return window.MarketPulseData.selectDays(days)}
function renderMarketState(state){
  const box=document.getElementById('mpContent');if(!box)return;
  if(state.snapshot){
    if(mpWindow!==state.snapshot.window_days){mpLimit=6;mpWindow=state.snapshot.window_days}
    renderMarketPulse(state.snapshot,state);
  }else box.innerHTML='<p class="market-status'+(state.error?' error-text':'')+'" id="mpSnapshotStatus" role="status">'+mpEsc(window.MarketPulseData.statusText(state))+'</p>';
  box.setAttribute('aria-busy',String(state.loading));
}

function renderMarketPulse(d,state=window.MarketPulseData.getState()){
  const box=document.getElementById('mpContent');if(!box)return;
  const levelClass=d.pressure_index>=70?'':d.pressure_index>=40?'med':'low';
  const topIntent=d.intent_mix?.[0];const topSegment=d.segment_mix?.[0],admin=Boolean(window.MarketPulseAccess?.isAdmin());
  const pressureLabel={LOW:'Düşük',MEDIUM:'Orta',HIGH:'Yüksek',CRITICAL:'Çok yüksek'}[d.pressure_level]||d.pressure_level;
  const summary=admin?d.executive_summary:d.move_count?`${d.window_days} günde ${d.move_count} rakip hamlesi kaydedildi. ${d.threat_count||0} hamle öncelikli değerlendirme gerektiriyor.`:'Bu dönemde yeni rakip hamlesi kaydedilmedi.';
  const moves=window.MarketPulseData.orderedMoves(d,mpOrder);
  const shown=moves.slice(0,mpLimit);
  box.className='mp-shell';
  box.innerHTML=`
    <p id="mpSnapshotStatus" class="market-status${state.error?' error-text':''}" role="status">${mpEsc(window.MarketPulseData.statusText(state))}</p>
    <div class="mp-hero">
      <article class="mp-pressure"><div class="mp-ring ${levelClass||'high'}" style="--score:${d.pressure_index||0}" aria-label="Rekabet baskısı ${d.pressure_index||0} / 100"><strong>${d.pressure_index||0}</strong></div><div class="mp-pressure-copy"><span>Rekabet Baskısı</span><b>${mpEsc(admin?d.pressure_level:pressureLabel)}</b><small>${d.window_days} günlük pencere</small></div></article>
      <article class="mp-summary"><span class="mp-label">Pazar Özeti</span><h2>Son ${mpEsc(d.window_days)} günde pazarda ne oluyor?</h2><p>${mpEsc(summary)}</p><div class="mp-badges" data-admin-only><span class="mp-badge">Rakip: ${mpEsc(d.competitor)}</span>${topIntent?`<span class="mp-badge">Ana niyet: ${mpEsc(topIntent.name)} · %${mpEsc(topIntent.pct)}</span>`:''}${topSegment?`<span class="mp-badge">Odak: ${mpEsc(topSegment.name)} · %${mpEsc(topSegment.pct)}</span>`:''}<span class="mp-badge">Açıklanabilir skor</span></div></article>
    </div>
    <div class="mp-kpis"><article class="mp-mini"><span class="mp-label">Rakip Hamlesi</span><strong>${d.move_count||0}</strong><small>${d.window_days} günde kaydedilen</small></article><article class="mp-mini"><span class="mp-label">Öncelikli</span><strong>${d.threat_count||0}</strong><small>Dikkat gerektiren hamle</small></article><article class="mp-mini"><span class="mp-label">Fırsat</span><strong>${d.opportunity_count||0}</strong><small>Değerlendirilebilir avantaj</small></article><article class="mp-mini"><span class="mp-label">İzlenecek</span><strong>${d.no_reaction_count||0}</strong><small>Takipte kalın</small></article></div>
    <div class="mp-main">
      <article class="mp-threats"><div class="mp-head"><strong>Öncelikli Rakip Hamleleri</strong><div class="mp-window" aria-label="Rakip hamlesi dönemi">${[7,30,90].map(days=>`<button data-market-days="${days}" class="${state.days===days?'active':''}" aria-pressed="${state.days===days}">${days} gün</button>`).join('')}</div></div><div class="mp-tools"><label for="mpMoveOrder">Sıralama<select id="mpMoveOrder"><option value="priority" ${mpOrder==='priority'?'selected':''}>Öncelik sırası</option><option value="recent" ${mpOrder==='recent'?'selected':''}>En yeni</option></select></label><a href="#competitor">Tüm değişiklikler →</a></div><p class="market-help" data-admin-only>Aynı taramada aynı pakette değişen alanlar tek hamlede birleştirilir. İzlenecek hareketler ve fırsatlar da listede yer alır.</p><div class="mp-list">${renderMoves(shown)}</div><div class="market-list-footer"><span id="mpMoveCount">${shown.length} / ${moves.length} ${admin?`gruplanmış hamle • ${Number(d.change_count)||0} alan değişikliği`:"hamle"}</span><button class="btn" data-market-more="moves" ${shown.length>=moves.length?'hidden':''}>Daha fazla hamle göster</button></div></article>
      <article class="mp-mix" data-admin-only><div class="mp-head"><strong>Rakip Strateji Haritası</strong><span class="muted">Niyet + segment</span></div><div class="mp-mixes"><div class="mp-mix-group"><div class="mp-mix-title">Stratejik Niyet</div>${renderMix(d.intent_mix||[])}</div><div class="mp-mix-group"><div class="mp-mix-title">Segment Odağı</div>${renderMix(d.segment_mix||[])}</div>${(d.opportunities||[]).length?`<div class="mp-mix-group"><div class="mp-mix-title">En Güçlü Fırsat</div><div class="mp-action"><b>${mpEsc(d.opportunities[0].product_name)}</b><br>${mpEsc(d.opportunities[0].action)}</div></div>`:''}</div></article>
    </div>
    <div class="mp-note" data-admin-only>${mpEsc(d.methodology)} • Skor karar desteğidir; müşteri tabanı, marj ve KKTCELL ürün benchmark’ı bağlandıkça V2’de kalibre edilecektir.</div>`;
}

function renderMoves(rows){
  if(!rows.length)return '<div class="mp-empty">Seçili dönemde kaydedilmiş rakip hamlesi yok.</div>';
  return rows.map(m=>{const c=m.threat>=70?'':m.threat>=40?'med':'low';return `<div class="mp-move" data-move-key="${mpEsc(m.key)}"><div class="mp-move-top"><div><div class="mp-move-title">${mpEsc(m.product_name)}</div><div class="mp-meta">${mpTime(m.detected_at)} • ${mpEsc(m.source_name)} • ${mpEsc(m.segment)} ${window.MarketPulseAccess?.isAdmin()?"• "+mpEsc(m.intent)+" ":""}• ${m.decision==='OPPORTUNITY'?'FIRSAT':m.decision==='NO_REACTION'?'İZLE':'TEHDİT'}</div></div><div class="mp-score ${c}">${mpEsc(m.threat)}/100<small>Tehdit skoru</small></div></div><ul class="mp-change-list">${(m.changes||[]).map(change=>'<li>'+window.MarketPulseData.changeMarkup(change)+'</li>').join('')}</ul><div class="mp-reasons" data-admin-only>${(m.reasons||[]).map(x=>`<span class="mp-reason">${mpEsc(x)}</span>`).join('')}</div><div class="mp-action"><b>Önerilen aksiyon:</b> ${mpEsc(m.action)}</div></div>`}).join('');
}
function renderMix(rows){return rows.slice(0,6).map(x=>`<div class="mp-bar-row"><span>${mpEsc(x.name)}</span><div class="mp-bar"><i style="width:${Math.max(3,x.pct)}%"></i></div><em>%${x.pct}</em></div>`).join('')||'<div class="muted">Veri bekleniyor.</div>'}

window.loadMarketPulse=loadMarketPulse;
installMarketPulse();
